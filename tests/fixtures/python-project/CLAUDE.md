# Python Project

Built with Django 4 and SQLAlchemy.

## Structure

- `src/app/` — Main application
- `src/models/` — Database models
- `tests/` — Test suite
- `src/missing_module/` — This does not exist

## Commands

```bash
python manage.py runserver
pip install -r requirements.txt
```
